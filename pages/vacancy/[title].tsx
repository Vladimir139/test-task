import type { NextPage } from "next";

import { EntityInfoPage } from "@/views";

const EntityInfo: NextPage = () => (
  // const router = useRouter();

  <EntityInfoPage />
);
export default EntityInfo;
